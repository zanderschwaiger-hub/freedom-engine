import React from 'react'
import { useScrollFade } from '../lib/useScrollFade'

export default function Mission() {
  const ref = useScrollFade()
  return (
    <section ref={ref} id="mission" style={{
      padding: 'clamp(72px, 11vw, 130px) clamp(24px, 8vw, 100px)',
      borderTop: '1px solid rgba(15,23,42,0.06)', background: 'var(--navy)'
    }}>
      <div style={{ maxWidth: '760px', margin: '0 auto' }}>
        <span className="section-tag fade-up">Our mission</span>
        <h2 className="fade-up" style={{
          fontFamily: "'Cormorant Garamond', serif", fontSize: 'clamp(32px, 4.5vw, 56px)',
          fontWeight: 600, lineHeight: 1.05, marginBottom: '20px'
        }}>
          Know what you have.<br />
          <em style={{ color: 'var(--accent)', fontStyle: 'italic' }}>Know what to do when something goes wrong.</em>
        </h2>
        <p className="fade-up" style={{
          fontSize: 'clamp(16px, 2vw, 19px)', fontWeight: 300, color: 'var(--dim)',
          lineHeight: 1.75, marginBottom: '28px'
        }}>
          Freedom Engine puts you and your team in control of your digital life. Individuals lock down the
          accounts their lives run on. Businesses prove their people are ready when something goes wrong.
          Real solutions to invisible problems — so you can focus on building, not on worrying about losing
          what you've built.
        </p>
        <a href="https://app.freedomengine.ca" target="_blank" rel="noreferrer" className="btn-ghost fade-up"
          style={{ fontSize: '15px' }}>
          Open the app →
        </a>
        <p className="fade-up" style={{
          marginTop: '36px', fontFamily: "'DM Mono', monospace", fontSize: '10px',
          letterSpacing: '3px', color: 'rgba(15,23,42,0.45)', textTransform: 'uppercase'
        }}>
          Digital safety through technical discipline.
        </p>
      </div>
    </section>
  )
}
